DATABASE_URL=file:./db/custom.db

# CSRF protection is disabled for local development (re-enable with ENABLE_CSRF=true).
ENABLE_CSRF=false

# Super Admin (auto-created in DB on server start)
SUPER_ADMIN_EMAIL=admin@localhost
SUPER_ADMIN_PASSWORD=inp
SUPER_ADMIN_NAME=Super Admin
